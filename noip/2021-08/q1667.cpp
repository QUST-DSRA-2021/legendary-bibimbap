#include <cstdio>
#include <cstdlib>
#include <cstring>
#include <iostream>
#include <queue>

using namespace std;
using namespace __gnu_cxx;

int m;
queue <int> val, buckets[11];

int main(int argc, char *argv[]) {
    scanf("%d", &m);
    for (int idx = 0; idx < m; ++idx)
        scanf("%d", val[idx]);
    for (int idx_bit = 0; idx_bit < 5; ++idx_bit) {
    }
    return 0;
}
